1. SELECT * FROM customers;

2. SELECT DISTINCT country FROM customers;

3. SELECT DISTINCT Customer's ID FROM customers WHERE Customers ID LIKE 'BL%';

4. SELECT * FROM orders LIMIT 100;

5. SELECT DISTINCT postal codes FROM customers WHERE postal code = '1010' 
OR postal code = '3012' OR postal code = '12209' OR postal code = '05023';

6. SELECT DISTINCT ShipRegion FROM orders WHERE ShipRegion IS NOT NULL;

7. SELECT * FROM customers
   ORDER BY country, city;

8. INSERT INTO customers(column1)
   VALUES ('Brandon Bath');

9. 

10. DELETE FROM orders
    WHERE order details = 1;

11. SELECT max(order details), min(order details), avg(order details) FROM orders;

12. SELECT max(order details), min(order details), avg(order details) FROM orders GROUP BY orderid;

13. 

14.

15.

16.



9. Update all ShipRegion to the value ‘EuroZone’ in the Orders table, where the
ShipCountry is equal to France. (Note: this requires safe updates to be turned off.
Search online for more info.)
13. Find the CustomerID that placed order 10290 (orders table)
14. Do an inner join, left join, right join on orders and customers tables. (These are
three separate queries, one for each type of join.)
15. Get employees’ firstname for all employees who report to no one.
16. Get employees’ firstname for all employees who report to Andrew.
